{
  "name": "ali-miniapp-scroll-load",
  "version": "0.0.2",
  "description": "支付宝小程序组件ScrollLoad",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git@github.com:if2er/ali-miniapp-scroll-load.git"
  },
  "files": [
    "es"
  ],
  "keywords": [
    "scroll-load",
    "mini-program"
  ],
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-ali": "^3.1.0",
    "eslint-plugin-import": "^2.11.0",
    "pre-commit-eslint": "^0.0.6",
    "rc-tools": "6.x"
  },
  "scripts": {
    "build": "rc-tools run compile && node scripts/cp.js && node scripts/rm.js",
    "pub": "git push origin && npm run build && npm publish"
  }
}
